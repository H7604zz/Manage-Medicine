<Window x:Class="ProjectPrn.View.Admin.NewTypeMedicine"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectPrn.View.Admin"
        mc:Ignorable="d"
        Title="NewTypeMedicine" Height="300" Width="350">
    <Grid Background="Cornsilk">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Content="New Type:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="10" Width="70"/>
                <TextBox x:Name="txtNewType" Grid.Row="0" Grid.Column="1" Margin="10" Width="100"/>
            </Grid>

            <Button Content="Add" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="10" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
