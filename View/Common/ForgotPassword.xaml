<Window x:Class="ProjectPrn.View.Common.ForgotPassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectPrn.View.Common"
        mc:Ignorable="d"
        Height="300" Width="400">
    <Grid Background="Cornsilk">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Content="Enter email:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="10"/>
                <TextBox x:Name="txtEmail" Grid.Row="0" Grid.Column="1" Margin="10" Width="180"/>
            </Grid>

            <Button Content="Send Request" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="10" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
