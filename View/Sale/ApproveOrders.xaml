<Window x:Class="ProjectPrn.View.Sale.ApproveOrders"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectPrn.View.Sale"
        mc:Ignorable="d"
        Title="ApproveOrders" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" x:Name="spnStatus" Orientation="Vertical"
                    HorizontalAlignment="Center"/>

        <Button Grid.Row="1" Width="100" Margin="0, 10, 0, 0"
                x:Name="btnConfirm" Click="btnConfirm_Click" >Order Confirmation</Button>
    </Grid>
</Window>
